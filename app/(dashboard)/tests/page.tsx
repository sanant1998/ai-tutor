import { TestsView } from "@/components/app/TestsView";

export const metadata = {
  title: "Tests · PaperPath",
  robots: { index: false, follow: false },
};

export default function TestsPage() {
  return <TestsView />;
}
